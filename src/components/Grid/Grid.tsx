import { FC } from 'react';
import { ReactNode } from 'react';

type Props = {
  children: ReactNode;
  title: string;
  subtitle?: string;
};

const Grid: FC<Props> = (props) => {
  return <div></div>;
};

export default Grid;
